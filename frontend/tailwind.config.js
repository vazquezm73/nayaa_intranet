// frontend/tailwind.config.js
export default {
  theme: {
    extend: {
      colors: {
        brand: {
          dark: '#0f172a',      // Azul casi negro (Header)
          primary: '#0062ff',   // Azul Nayaa (Botones)
          accent: '#00d1ff',    // Celeste (Detalles)
          muted: '#64748b',     // Gris para textos secundarios
          bg: '#f1f5f9',        // Gris muy claro para el fondo general
        }
      }
    },
  },
}
